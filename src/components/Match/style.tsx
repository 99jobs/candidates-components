import { styled } from '../../configs/stitches.config'

export const StyledMatch = styled('div', {
  width: 37,
  fontSize: '$small',
  fontWeight: 600,
  color: '$onGrayDark',
  textAlign: 'center',
})
