import * as PrimitiveNavigationMenu from '@radix-ui/react-navigation-menu'
import { styled } from '../../configs/stitches.config'

export const StyledNavMenuListItem = styled(PrimitiveNavigationMenu.Item, {
  listStyle: 'none',
})
