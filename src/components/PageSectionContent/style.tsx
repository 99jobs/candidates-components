import { styled } from '../../configs/stitches.config'

export const StyledPageSectionContent = styled('div', {
  fontSize: '$body',
  color: '$gray',
  paddingX: 10,

  p: {
    marginBottom: 8,
  },
})
