import { Canvas, Meta, ColorPalette, ColorItem } from '@storybook/addon-docs'
import { config } from './configs/stitches.config'

<Meta title="Intro" />

# Cores

<ColorPalette>
  {Object.keys(config.theme.colors).map((k) => {
    return <ColorItem title={k} colors={{ [k]: config.theme.colors[k] }} />
  })}
</ColorPalette>
